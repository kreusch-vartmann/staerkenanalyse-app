# Test-Dependencies für Stärkenanalyse-App Test-Suite
# Installiere mit: pip install -r requirements-test.txt

# Core Testing Frameworks
pytest>=8.0.0
pytest-flask>=1.3.0
pytest-cov>=4.1.0

# Mocking & Fixtures
pytest-mock>=3.12.0

# HTML Coverage Reports
coverage[toml]>=7.4.0

# Zusätzliche Testing-Tools
pytest-xdist>=3.5.0  # Parallele Test-Ausführung
pytest-timeout>=2.2.0  # Test-Timeouts
pytest-randomly>=3.15.0  # Randomisierte Test-Reihenfolge

# Assertions & Matchers
dirty-equals>=0.7.0  # Flexible Assertions für komplexe Datenstrukturen

# Freeze Time für Datum-Tests (optional)
freezegun>=1.4.0

# Faker für Test-Datengenierung (optional)
faker>=22.0.0

# HTTP Request Mocking (optional, falls externe APIs getestet werden)
responses>=0.24.0
requests-mock>=1.11.0

# Playwright für E2E-Tests (optional, nur wenn Browser-Tests benötigt werden)
# playwright>=1.40.0
# pytest-playwright>=0.4.4

# Hinweis: Die Haupt-Dependencies (Flask, SQLAlchemy, etc.) sollten bereits
# über requirements.txt installiert sein. Diese Datei enthält nur zusätzliche
# Test-spezifische Packages.

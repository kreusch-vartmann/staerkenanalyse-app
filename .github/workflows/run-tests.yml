# ⚠️ DEPRECATED: Dieser Workflow wurde durch tests.yml ersetzt
# Grund: init_test_db.py existiert nicht, doppelte Test-Ausführung
# Status: DISABLED - nutze stattdessen .github/workflows/tests.yml

name: Test Suite (DEPRECATED)

on: {}

jobs:
  placeholder:
    name: This workflow is deprecated
    runs-on: ubuntu-latest
    steps:
      - name: Info
        run: echo "ℹ️ Use .github/workflows/tests.yml instead"
